# Dependências da edição COMPLETA (com IA) do Tarjador Desktop.
#
# = requirements-lite.txt + a pilha de IA, com OS MESMOS PINS de produção
# (requirements.txt da raiz): torch 2.13.0+cpu e transformers 5.13.0. Não é
# zelo vazio — o desktop não deve rodar uma combinação de versões que nunca
# existiu em produção.
streamlit>=1.41,<1.60
pyarrow>=16,<20
numpy>=2.0,<2.3
pandas>=2.2,<2.3
pymupdf>=1.25,<1.26
presidio-analyzer>=2.2
phonenumbers>=8.13
spacy>=3.8,<3.9
https://github.com/explosion/spacy-models/releases/download/pt_core_news_sm-3.8.0/pt_core_news_sm-3.8.0-py3-none-any.whl
pydantic>=2.10
pydantic-settings>=2.7

# IA de nomes (BERT LeNER-Br). O sufixo "+cpu" só existe no índice do
# PyTorch e força o build CPU — o wheel CUDA tem gigabytes e não serve para
# nada aqui.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
transformers==5.13.0

# Só do build (não vai para o bundle).
pyinstaller>=6.14,<7
